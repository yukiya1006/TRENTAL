<div class="absolute inset-x-0">
  <div id='map' class="opacity-50 absolute inset-x-0"></div>
  <%= render 'maps/map' %>
  <div class="container px-5 py-24 mx-auto flex">
    <div class="lg:w-1/3 md:w-1/2 bg-white rounded-lg p-8 flex flex-col md:ml-auto w-full mt-10 md:mt-0 relative z-10 shadow-md">
      <h2 class="text-gray-900 text-lg mb-1 font-medium title-font">最寄りのジムを登録してみましょう</h2>
      <p class="leading-relaxed mb-5 text-gray-600">※正式名称で登録しないと動作が上手くいかないことがありますので、必ず店舗名をご確認の上ご登録お願いいたします</p>
      <div class="relative mb-4">
        <%= form_for(@map, :url => { controller:'maps', action:'create'})do |f| %>
        <%= f.label :店舗名 %>
        <%= f.text_field :address, placeholder: "ジムの正式名称＋〇〇店 ※スペースなし", class: "w-full bg-white rounded border border-gray-300 py-1 px-3" %>
        <%= f.label :料金設定 %>
        <%= f.text_field :body, placeholder: "例: １時間/５０００円", class: "w-full bg-white rounded border border-gray-300 py-1 px-3" %>

      　<%= f.label :tag_list %>
      　<%= f.text_field :tag_list, value: @map.tag_list.join(','), class: "form-control" %>


      　<%= f.submit "Register", class: "text-white bg-indigo-500 border-0 py-2 px-6 focus:outline-none hover:bg-indigo-600 rounded text-lg" %>
    　　<% end %>
      </div>
    </div>
  </div>
</div>