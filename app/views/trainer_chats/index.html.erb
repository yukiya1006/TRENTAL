<div class="text-4xl font-bold text-center">Trainer Chat Room Lists</div>
<div class="py-10 px-10 md:flex md:flex-wrap justify-center">
<% @rooms.each do |room| %>
    <p><%= link_to room.user.name, trainer_chat_path(room.id), class: "mr-4" %></p>
<% end %>
</div>